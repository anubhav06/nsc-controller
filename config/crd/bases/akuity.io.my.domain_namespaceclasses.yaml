---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: namespaceclasses.akuity.io.my.domain
spec:
  group: akuity.io.my.domain
  names:
    kind: NamespaceClass
    listKind: NamespaceClassList
    plural: namespaceclasses
    singular: namespaceclass
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NamespaceClass is the Schema for the namespaceclasses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NamespaceClassSpec defines the desired state of NamespaceClass
            properties:
              resources:
                description: Resources is a list of Kubernetes objects that should
                  be created when a namespace of this class is created.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                minItems: 1
                type: array
            required:
            - resources
            type: object
          status:
            description: NamespaceClassStatus defines the observed state of NamespaceClass
            properties:
              resources:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
            required:
            - resources
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
